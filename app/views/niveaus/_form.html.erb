<%= form_for(@niveau) do |f| %>
  <% if @niveau.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@niveau.errors.count, "error") %> prohibited this niveau from being saved:</h2>

      <ul>
      <% @niveau.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lib_niv %><br>
    <%= f.text_field :lib_niv %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
